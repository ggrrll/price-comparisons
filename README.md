# Price comparison

Basic `streamlit` app for tariffs comparison, in particular for gas prices  

## 🐍 python environment

* install `uv` : `curl -LsSf https://astral.sh/uv/install.sh | sh` 

* sync the python env : `uv sync` 